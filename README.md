# Telegram AI Assistant

[![Python 3.13](https://img.shields.io/badge/python-3.13-blue?logo=python&logoColor=white)](https://www.python.org/)
[![Ruff](https://img.shields.io/badge/lint-ruff-261230?logo=ruff&logoColor=white)](https://docs.astral.sh/ruff/)
[![mypy](https://img.shields.io/badge/types-mypy-blue)](https://mypy-lang.org/)
[![pytest](https://img.shields.io/badge/tests-pytest-0A9EDC?logo=pytest&logoColor=white)](https://docs.pytest.org/)

Telegram-бот на Python, который превращает естественный текст в
структурированные действия. Бот создаёт задачи в Linear, работает с
Google Calendar, отвечает на обычные сообщения и получает личные сообщения
через Telegram Business.

## Требования

- Python 3.13
- [uv](https://docs.astral.sh/uv/)
- токен Telegram-бота и ключ совместимого с OpenAI LLM API
- персональный Linear API-ключ и UUID команды
- Google OAuth credentials и публичный HTTPS callback URL

## Быстрый старт

```bash
git clone <repository-url>
cd personal_ai_assistent
make install
```

Создайте `.env` на основе примера:

```bash
cp .env.example .env
```

На Windows вместо этого:

```powershell
Copy-Item .env.example .env
```

Заполните значения:

```env
TELEGRAM_BOT_TOKEN=your-telegram-token
TELEGRAM_ALLOWED_USER_ID=your-numeric-telegram-user-id
LLM_API_KEY=your-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-5.6
GOOGLE_OAUTH_CLIENT_ID=your-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret
GOOGLE_OAUTH_REDIRECT_URI=https://your-domain.example/oauth/google/callback
GOOGLE_TOKEN_ENCRYPTION_KEY=your-fernet-key
LINEAR_API_KEY=your-linear-api-key
LINEAR_TEAM_ID=your-team-uuid
```

## Linear

Для просмотра используйте команды в меню бота:

- `/tasks` — все неархивные задачи настроенной команды Linear, включая
  выполненные, со статусом, сроком, приоритетом и ссылкой.
- `/calendar` — события основного Google Calendar, включая события на весь день.
  Без указания периода — ближайшие 365 дней. Время отображается в `APP_TIMEZONE`.
- `/agenda` — задачи Linear и ближайшие события календаря одним запросом.

По умолчанию на странице 10 записей. Inline-кнопки делают только одно —
листают страницы: «Назад», «Далее» и, для списков длиннее трёх страниц,
переход в начало и конец. У списка из одной страницы кнопок нет вовсе.
В заголовке видны фильтры, период, сортировка, общее количество и номер страницы.

Всё остальное — фильтры, сортировка, размер страницы, обновление данных,
изменение и удаление — задаётся обычным сообщением, которое разбирает LLM.

Фильтры можно задать обычным сообщением или аргументами команды:

- «Покажи события за 2026 год»;
- `/tasks открытые, где есть слово отчёт, сначала ближайший срок`;
- «Выполненные задачи за последний год»;
- «Дела по теме ремонта»;
- «Задачи проекта Сайт с меткой Bug, сначала срочные»;
- `/calendar за прошлый месяц по названию`;
- «А теперь только за прошлый год» — уточнение предыдущего запроса.

Текст ищется без учёта регистра в названии и описании, у событий — также в месте.
Поиск по теме использует предложенные агентом ключевые слова; они показываются
в фильтрах. Буквальный запрос «есть слово X» ищет подстроку X. Linear поддерживает
фильтры статуса, проекта, имени исполнителя, метки, приоритета и архива. Период
задач можно применить к дате создания, обновления, завершения или сроку выполнения.
Без уточнения используется дата создания; «выполненные за год» — дата завершения.
У событий выбираются все встречи, пересекающие период, включая повторяющиеся.
Правая граница периода не включена: весь 2026 год — до 1 января 2027 года.

Сортировка применяется ко всей найденной выборке перед разбивкой на страницы.
Актуальные данные получает каждый новый запрос; листание использует
сохранённую выборку, чтобы записи не перескакивали между страницами. Чтобы
обновить данные, повторите запрос сообщением. Списки
действуют до часа, до перезапуска бота или вытеснения более новыми запросами.
Устаревшая кнопка предлагает повторить запрос. При превышении 5000 прочитанных
событий/найденных задач или времени поиска бот просит сузить фильтры, не выдавая
усечённый список за полный.

Если одна интеграция недоступна, `/agenda` покажет данные второй и сообщение
об ошибке. Для подключения календаря используйте `/calendar_connect`.

Создайте [персональный API-ключ](https://linear.app/developers/graphql) в
**Settings → Account → Security & Access** и
разрешите ему чтение, создание и удаление issues. UUID команды можно скопировать в Linear
через `Cmd/Ctrl+K` → **Copy model UUID**. Запишите значения в
`LINEAR_API_KEY` и `LINEAR_TEAM_ID`.

Когда бот распознаёт одно или несколько действий `create_task`, он создаёт
отдельный issue для каждого дела в исходном порядке и возвращает идентификатор
и ссылку для каждой задачи. В одном сообщении можно сочетать создание задач и
событий календаря. Один
ключ и команда общие для экземпляра бота, поэтому свободный текст, LLM и
создание Linear-задач доступны только Telegram-пользователю с ID из
`TELEGRAM_ALLOWED_USER_ID`.

Для удаления напишите «Удали задачу ENG-42», «Удали задачу купить молоко»
или «Удали все задачи в Linear». Бот покажет найденные задачи и inline-кнопки
«Удалить» / «Отмена». При совпадении названий можно выбрать конкретную задачу.
«Все задачи» означает задачи настроенной команды, включая архивные.

## Telegram Business

1. Включите **Business Mode** для бота через `@BotFather`.
2. В Telegram откройте **Settings → Chat Automation** (в старых клиентах —
   **Telegram Business → Chatbots**), подключите бота и выберите чаты, к которым
   он получит доступ.
3. Разрешите боту чтение сообщений. ID подключаемого аккаунта должен совпадать
   с `TELEGRAM_ALLOWED_USER_ID`.

Приложение подписывается на `business_connection`, `business_message` и
`edited_business_message`. Оно
проверяет владельца подключения, получает входящие личные сообщения и помечает
их прочитанными. Текст и подписи входящих сообщений, ответов владельца и
бизнес-бота сохраняются в отдельном контексте каждого собеседника. Исходящие
сообщения не помечаются прочитанными. Редактирование обновляет сохранённую запись.
После **7 секунд тишины в чате** отдельный LLM-анализатор извлекает новые
договорённости из последних 60 сообщений (в пределах лимита контекста).
Задачи создаются в Linear, встречи с точным временем — в Google Calendar,
важные факты сохраняются как зашифрованные заметки в SQLite. Уведомления со
ссылками и результатами получает только владелец в личном чате с ботом;
сообщения клиенту не отправляются. Владелец должен заранее запустить бота через `/start`.

Анализатор допускает только `create_task`, `create_event` и `save_note`.
Задачи создаются сразу по прямому поручению собеседника владельцу — без
подтверждения владельца, в том числе по напоминанию о работе. Обещания и
согласие самого владельца выполнить работу также создают задачи. Поручения
владельца собеседнику или третьим лицам, их обещания и одобрение их планов
не становятся задачами владельца. Анализатор определяет исполнителя по всему
диалогу и учитывает, на какое сообщение дан Telegram-ответ. При неясном
исполнителе задача не создаётся; короткое «да» требует ссылки на конкретную
просьбу собеседника к владельцу. Для встречи по-прежнему
требуется новая реплика владельца с согласием или назначением.
Автор и источник поручения проверяются по Telegram ID и точной цитате, а не имени.
Реплики бизнес-ботов исключаются; старые записи без ID автора не исполняются.
Инструкции управления ботом и интеграциями не превращаются в действия.
Важные факты можно сохранять без согласия.

SQLite хранит указатель обработанных сообщений, неизменяемый план действий,
их результаты и очередь уведомлений. Повторная доставка и редактирование уже
обработанного сообщения не создают новые объекты. Новое согласие следует
отправлять отдельным сообщением. После перезапуска незавершённые планы и
уведомления продолжают обрабатываться. При неопределённом результате запроса
к Linear/Calendar автоматического повтора нет: владелец получает сообщение
с просьбой проверить результат вручную. Сбои анализа и доставки повторяются
до трёх раз; затем обработка возобновляется при новом сообщении или перезапуске.

Заметки и результаты хранятся в зашифрованных записях `business_actions`;
очистка или сжатие контекста не удаляют этот архив и созданные внешние объекты.
LLM определяет исполнителя и смысл реплик, поэтому неоднозначные формулировки могут быть пропущены.

## Контекст чатов

Бот запоминает ваши сообщения и свои ответы и передаёт историю этого чата
модели при следующем обращении. Контексты общения с ботом и Business-чатов
разделены: переписка с одним собеседником не добавляется автоматически в другую.
Для Business сохраняются только сообщения, полученные после подключения этой
версии бота; загрузки старой переписки и распознавания вложений нет.

На каждый чат хранится до **60 сообщений** и **24 000 символов** контекста
(включая резюме, имена и даты). При превышении любого лимита удаляются самые
старые сообщения. Одна длинная запись сокращается до 6 000 символов с отметкой.
Лимит по символам дополняет лимит сообщений и не зависит от токенизатора модели.
Контекст и названия чатов шифруются тем же ключом, что токены календаря, и
сохраняются в SQLite по `DATABASE_PATH`; перезапуск их не сбрасывает.

Управление доступно владельцу в личном чате с ботом:

Все поддерживаемые команды доступны по кнопке **«Меню»** рядом с полем
сообщения, включая команды календаря. Список обновляется при запуске бота;
устаревшая `/profile` удалена. Неизвестные команды не отправляются в LLM.

- `/clear` — выбрать один или несколько чатов и очистить сообщения и резюме.
- `/compact` — выбрать чаты и заменить их текущий контекст резюме до
  **4 000 символов**. Следующие сообщения добавляются к резюме; следующее сжатие
  учитывает его. При ошибке модели прежний контекст сохраняется.
- `/history` — посмотреть текущие сообщения и резюме выбранного чата с
  перелистыванием страниц или скачать контекст всех чатов файлом `.txt`.

Кнопка **«Выбрать все чаты»** в `/clear` и `/compact` отмечает все чаты из
открытого меню, включая чат с ботом. Затем нажмите кнопку действия. Меню
действует 15 минут и закрывается при перезапуске. Сжатие вызывает LLM отдельно
для каждого выбранного непустого чата. Переписка в Telegram остаётся на месте.
`/history` показывает только сохранённый сейчас контекст: записи, удалённые
лимитом или очисткой, восстановить из него нельзя. Для сохранения важных фактов
используйте `/compact` до достижения лимита.

## Google Calendar

Создайте OAuth Client ID типа **Web application** в Google Cloud Console, включите
Google Calendar API и добавьте `GOOGLE_OAUTH_REDIRECT_URI` в список Authorized
redirect URIs. Для шифрования токенов создайте ключ:

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

Приложение запускает HTTP-сервис на `HTTP_HOST:HTTP_PORT` (по умолчанию
`0.0.0.0:8080`), который должен быть доступен снаружи по HTTPS через reverse
proxy. В Telegram используйте `/calendar_connect`, затем `/calendar` для
ближайших событий. Для отключения аккаунта используйте `/calendar_disconnect`.

Изменение события задаётся сообщением: «Перенеси стоматолога на завтра в 16:00».
Бот ищет событие по текущему названию (подстрока без учёта регистра). Если
подходит несколько событий, бот перечислит их и попросит уточнить, а не выберет
сам; если не найдено ни одного — попросит уточнить название.

События на весь день показываются с датой или диапазоном дат и пометкой
«весь день». Их можно открыть по ссылке или удалить с подтверждением.

Удалять события можно и текстом: «Удали событие Стоматолог» или
«Удали все события в календаре». Бот показывает список и количество перед
подтверждением. Полное удаление относится к основному Google Calendar,
включая прошедшие события, события на весь день и повторяющиеся серии.
Удаление серии явно отмечено в списке. При совпадении названий появляются
отдельные подтверждения для выбора нужного события.

Подтверждение удаления действует 15 минут, привязано к пользователю и
срабатывает один раз. «Подтверждаю» в тексте не заменяет кнопку.
Удаляются только объекты из показанного списка; новые задачи и события,
созданные после запроса, в него не добавляются. При частичной ошибке бот
сообщает число удалённых объектов и перечисляет неподтверждённые результаты.

Reverse proxy не должен сохранять query string OAuth callback в access logs.
Одноразовые состояния и кнопки календаря привязаны к
`TELEGRAM_ALLOWED_USER_ID`, имеют TTL и хранят event ID только на сервере.

Запустите бота:

```bash
make run
```

## Docker

Соберите образ:

```bash
make docker-build
```

Запустите бота в фоне, передав настройки из `.env`:

```bash
make docker-run
```

Команда публикует порт `8080` для OAuth callback и хранит SQLite-базу в
именованном Docker volume, поэтому подключения календарей переживают пересоздание
контейнера.

При повторном запуске предыдущий контейнер с тем же именем будет заменён. Для просмотра логов и остановки используйте:

```bash
make docker-logs
make docker-stop
```

Для запуска нужен работающий Docker Engine (Docker Desktop на Windows).

## Разработка

`Makefile` использует `uv`, поэтому команды работают одинаково в Windows, Linux и macOS.

| Команда | Назначение |
| --- | --- |
| `make install` | Установить зависимости, включая инструменты разработки |
| `make run` | Запустить бота |
| `make lint` | Проверить стиль и ошибки Ruff |
| `make format` | Отформатировать код и применить безопасные исправления Ruff |
| `make typecheck` | Запустить строгую проверку типов mypy |
| `make test` | Запустить pytest с branch coverage и обязательным порогом 90% |
| `make eval` | Оценить сценарии промптов и интеграций через DeepEval и DeepInfra GLM-5.3-Flash |
| `make check` | Запустить все проверки: Ruff, mypy и pytest |
| `make docker-build` | Собрать Docker-образ `personal-ai-assistant` |
| `make docker-run` | Собрать и запустить бота в Docker с `.env` |
| `make docker-logs` | Следить за логами контейнера |
| `make docker-stop` | Остановить контейнер |

Перед отправкой изменений выполните:

```bash
make check
```

DeepEval-тесты и настройка отдельного LLM-судьи описаны в
[`evals/README.md`](evals/README.md). Обычные проверки работают без API-запросов;
`make eval` требует `DEEPINFRA_API_KEY` и вызывает модель приложения и судью.

Тот же gate запускается в GitHub Actions; дополнительно CI выполняет Bandit и
pip-audit. Результаты последнего предрелизного ревью находятся в
[`docs/audit/CODE_AUDIT_REPORT_2026-09-04.md`](docs/audit/CODE_AUDIT_REPORT_2026-09-04.md).

## Структура

```text
src/
├── application/    # use cases, сервисы и порты
├── domain/         # модели и типы действий
├── infrastructure/ # реализации LLM- и Telegram-адаптеров
├── config.py       # настройки окружения
└── main.py         # запуск приложения
tests/              # автоматические тесты
```
