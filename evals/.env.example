# Copy to .env.eval in the repository root. Keep real keys out of Git.
DEEPINFRA_API_KEY=
EVAL_JUDGE_MODEL=zai-org/GLM-5.3-Flash
EVAL_TIMEOUT_SECONDS=90

# The application model under test uses LLM_API_KEY, LLM_BASE_URL and LLM_MODEL
# from .env. Override those in .env.eval only when evaluating another model.
